This directory vendors the upstream `gif.js` browser distribution from `gif.js@0.2.0`.

Included files:
- `gif.js`
- `gif.worker.js`
- `LICENSE`

The files are used to support local looping GIF export for repeatable bar styles without relying on a runtime CDN.
